import 'package:flutter/material.dart';

class WorkshopManagerStudent extends StatelessWidget {
  const WorkshopManagerStudent({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Workshop Page'),
    );
  }
}
